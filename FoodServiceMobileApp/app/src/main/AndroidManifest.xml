<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jaren.foodservice_mobileapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".BusinessMenuActivity" />
        <activity android:name=".ClientMenuActivity" />
        <activity android:name=".BusinessCreatePackageActivity" />
        <activity android:name=".BusinessAccountActivity" />
        <activity android:name=".ClientCheckPackageActivity" />
        <activity android:name=".ClientAccountActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RegisterBusinessActivity" />
        <activity android:name=".RegisterClientActivity" />
        <activity android:name=".BusinessClaimablePackagesActivity" />
        <activity android:name=".ClientClaimPackageActivity"></activity>


    </application>

</manifest>